
# References

`/Users/molnarr2/Projects/antioch/antioch`
`apps/mobile`
# Instructions

1. Copy over the `src/library` from antioch to our `apps/mobile` app to the same location.
2. Copy over the `antioch/src/core/plugin` to the same location.
3. Schema:
	1. The `SchemaDefinition.ts` file same place, but it needs to be trimmed down and with example but commented in it for an LLM. Short and concise.
	2. The same for the `antioch/src/library/cloudplatform/firebase/Schema.ts` it needs to be trimmed down and with example code in it commented for an LLM. Short and concise.
	3. The `src/schema/object` and `src/schema/type` need to be copied over.