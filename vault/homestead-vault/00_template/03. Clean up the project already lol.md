# Instructions

1. We want to completely remove the src/core/plugin folder.
2. Analyze the `apps/mobile/src/library/cloudplatform/firebase` We'll remove the files and instead create the following:
	1. Create a firebase authentication helper class for authentication, can be similar to the FirebaseAuth service. We do want an interface within that same file.
3. Remove the `apps/mobile/src/library/permission`
4. Remove the `apps/mobile/src/library/social`
5. Remove the `apps/mobile/src/library/storage`
6. Remove the `apps/mobile/src/schema/SchemaDefinition.ts`

# Guidelines

* Keep MMkv package we'll be using that.
* Keep the permission package that'll be used almost always.
* Remove the socal package.
