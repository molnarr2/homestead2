
# References

`/Users/molnarr2/Projects/ephesus/EphesusApp`
`apps/mobile`
# Instructions
1. Within the EphesusApp we have `theme/` folder with colors, spacing, and typography We want those files within this new mobile app. Copy them over.
2. Within the `src\components` there is button, dialog, input, and modal (just those) we want them copied over
3. We want the `src/feature/debug` to contain ALL of the components from above, text (all three colors, with different sizes, etc..), background color, etc..
	1. See this as an example: EphesusApp/src/feature/debug/DebugScreen.tsx
	2. I also want an example of the text with the three colors on all three of the colors as background so we can see how they all look.
	3. We also want to see what the colors as text looks like on white and black backgrounds.
4. The main app.tsx file should have a button which goes to this page.
5. I want this mobile app to use the same navigation as the `EphesusApp/src/navigation` for routing pages, etc..