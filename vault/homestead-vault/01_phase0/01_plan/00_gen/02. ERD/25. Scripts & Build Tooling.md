# Goal

Create and update all local scripts in `/scripts` for running, cleaning, deploying, and managing the Homestead project. Also includes Firebase deploy scripts for rules and functions.

# References

- `~/Projects/ephesus/ephesus-vault/01. MVP0/01. Prompts/01. llm/generated/23. Scripts & Build Tooling.md` - Reference pattern
- `firebase-rule/` - Rules to deploy
- `functions/` - Functions to deploy

# Instructions

## 1. Scripts

### `scripts/run-ios.sh`
```bash
#!/bin/bash
set -e
echo "Running Homestead on iOS..."
SIMULATOR="${1:-iPhone 16}"
cd "$(dirname "$0")/../HomesteadApp"
npx react-native run-ios --simulator "$SIMULATOR"
```

### `scripts/run-android.sh`
```bash
#!/bin/bash
set -e
echo "Running Homestead on Android..."
cd "$(dirname "$0")/../HomesteadApp"

if [ -z "$JAVA_HOME" ]; then
  echo "Error: JAVA_HOME is not set"
  exit 1
fi
if [ -z "$ANDROID_HOME" ]; then
  echo "Error: ANDROID_HOME is not set"
  exit 1
fi

npx react-native run-android
```

### `scripts/clean-ios.sh`
```bash
#!/bin/bash
set -e
echo "Cleaning iOS build..."
cd "$(dirname "$0")/../HomesteadApp"
cd ios
rm -rf build Pods Podfile.lock
pod cache clean --all
cd ..
rm -rf node_modules
npm install
cd ios
pod install
echo "iOS clean complete."
```

### `scripts/clean-android.sh`
```bash
#!/bin/bash
set -e
echo "Cleaning Android build..."
cd "$(dirname "$0")/../HomesteadApp"
cd android
./gradlew clean
cd ..
rm -rf node_modules
npm install
echo "Android clean complete."
```

### `scripts/deploy-rules.sh`
```bash
#!/bin/bash
set -e
echo "Deploying Firestore and Storage rules..."
cd "$(dirname "$0")/.."
firebase deploy --only firestore:rules,storage --project <PROJECT_ID>
echo "Rules deployed."
```

### `scripts/deploy-functions.sh`
```bash
#!/bin/bash
set -e
echo "Building Cloud Functions..."
cd "$(dirname "$0")/../functions"
npm run build
echo "Deploying Cloud Functions..."
cd "$(dirname "$0")/.."
firebase deploy --only functions --project <PROJECT_ID>
echo "Functions deployed."
```

### `scripts/deploy-all.sh`
```bash
#!/bin/bash
set -e
echo "Deploying everything..."
"$(dirname "$0")/deploy-rules.sh"
"$(dirname "$0")/deploy-functions.sh"
echo "All deployments complete."
```

### `scripts/test.sh`
```bash
#!/bin/bash
set -e
echo "Running tests..."
cd "$(dirname "$0")/../HomesteadApp"
npm test
echo "Tests complete."
```

## 2. Script Permissions

All scripts must be executable:
```bash
chmod +x scripts/*.sh
```

## 3. Firebase Project Setup

Create `firebase.json` **at the project root** (`/homestead2/firebase.json`):
```json
{
  "firestore": {
    "rules": "firebase-rule/firestore.rules",
    "indexes": "firebase-rule/firestore.indexes.json"
  },
  "storage": {
    "rules": "firebase-rule/storage.rules"
  },
  "functions": {
    "source": "functions"
  }
}
```

## 4. Project Root Structure (final)

```
homestead2/
  .claude/
  .git/
  .gitignore
  CLAUDE.md
  firebase.json
  vault/
    homestead-vault/
      00_research/
  HomesteadApp/
    src/
      Bootstrap.ts
      components/
      core/
      feature/
      library/
      navigation/
      schema/
      store/
      theme/
      types/
      util/
    App.tsx
    global.css
    metro.config.js
    package.json
    tsconfig.json
    ios/
    android/
  firebase-rule/
    firestore.rules
    storage.rules
    firestore.indexes.json
  functions/
    src/
    package.json
    tsconfig.json
  scripts/
    run-ios.sh
    run-android.sh
    clean-ios.sh
    clean-android.sh
    deploy-rules.sh
    deploy-functions.sh
    deploy-all.sh
    test.sh
```

# Notes

- Replace `<PROJECT_ID>` with actual Firebase project ID once created
- Scripts use `set -e` to fail fast on errors
- All scripts use relative paths from the `scripts/` directory via `$(dirname "$0")`
- No CI/CD for MVP — manual deploys via scripts
- Consider adding a `scripts/setup.sh` for first-time developer setup (install deps, pods, configure Firebase)
