# Goal

Master guide for implementing the Homestead app using the TDD (Technical Design Document) plan documents. Defines the execution order, what to include in each prompt, validation checkpoints between layers, and rules for the LLM to follow.

**TDD Document Root:** `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/`

# How to Use This Guide

Each implementation step below is a **single LLM prompt session**. For each step:

1. Copy the listed documents into the prompt context
2. Include the prompt instruction provided
3. Validate the output against the checkpoint criteria before moving to the next step
4. If validation fails, fix issues in the same session before proceeding

**Do not skip layers.** Each layer depends on the output of previous layers.

---

# Layer 1: Project Setup & Scaffold

Project setup, dependencies, theme configuration, shared UI components, navigation shell, and the FirebaseAuth library. See `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/01. Project Setup & Dependencies.md` for the full dependency list and target directory structure.

---

# Layer 2: Schema & Utilities

## Step 2.1 — Shared Schema & Utilities

**Include in prompt:**
- `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/02. Shared Schema Definitions.md`
- `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/03. Utility & Foundation Layer.md`

**Prompt instruction:**
> Implement the shared schema definitions and foundation utility layer. Create all TypeScript interfaces and default factory functions as specified. Each schema file contains only the interface + `{entity}_default()` function. Services handle Firestore access directly. Split models into separate files per entity.

**Validation checkpoint:**
- [ ] All schema interfaces exist in `apps/mobile/src/schema/` with correct directory structure
- [ ] Every schema has `admin: AdminObject` field
- [ ] Default factory functions return correct defaults (e.g., `animal_default()`, `careEvent_default()`)
- [ ] `DateUtility.ts` and `StringUtility.ts` exist in `apps/mobile/src/util/`
- [ ] Domain logic utilities exist: `GestationUtility.ts`, `WithdrawalUtility.ts`, `CareUtility.ts`, `AnimalUtility.ts`, `ProductionUtility.ts`
- [ ] TypeScript compiles with no errors

---

# Layer 3: Services Layer

## Step 3.1 — Feature Services

**Include in prompt:**
- `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/04. Services Layer.md`

**Prompt instruction:**
> Implement the feature domain services. Each service lives at `apps/mobile/src/feature/{domain}/service/` with an interface I{Domain}Service.ts and implementation {Domain}Service.ts. Services call `@react-native-firebase/firestore` and `@react-native-firebase/storage` directly. Use onSnapshot for real-time collections (animals, care events, active breedings, active withdrawals). Services return `Promise<IResult>` for mutations. Create Bootstrap.ts to instantiate and export all services.

**Validation checkpoint:**
- [ ] All service interfaces and implementations exist in `apps/mobile/src/feature/{domain}/service/`
- [ ] Services import `@react-native-firebase/firestore` directly
- [ ] `onSnapshot` used for: animals, care events, active breedings, active withdrawals
- [ ] All mutations return `Promise<IResult>`
- [ ] `Bootstrap.ts` exists at `apps/mobile/src/Bootstrap.ts` with all `bs{Service}` exports
- [ ] TypeScript compiles with no errors

---

# Layer 4: State Management

## Step 4.1 — Zustand Stores

**Include in prompt:**
- `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/13. Foundation Context Reference.md`
- `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/06. State Management with Zustand.md`

**Prompt instruction:**
> Implement all Zustand stores as specified. Each store should have state, actions that call services (from Bootstrap), and a clear() function. Create the resetAllStores utility.

**Validation checkpoint:**
- [ ] All stores exist in `apps/mobile/src/store/`: authStore, userStore, animalStore, careStore, healthStore, breedingStore, productionStore, noteStore, weightStore, animalTypeStore
- [ ] Each store has a `clear()` function
- [ ] `resetAllStores.ts` calls `clear()` on every store
- [ ] Stores call services from Bootstrap (not Firebase directly)
- [ ] TypeScript compiles with no errors

---

# Layer 5: Data Layer (Firestore & Functions)

## Step 5.1 — Firestore Schema & Rules

**Include in prompt:**
- `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/07. Firestore Schema & Security Rules.md`

**Prompt instruction:**
> Create the Firestore security rules, storage rules, and indexes file in the firebase-rule/ directory. All collections use flat root-level design with userId for ownership. AnimalType has breed, careTemplate, and eventTemplate subcollections.

**Validation checkpoint:**
- [ ] `firebase-rule/firestore.rules` exists with userId-based security
- [ ] `firebase-rule/storage.rules` exists
- [ ] `firebase-rule/firestore.indexes.json` exists with all composite indexes
- [ ] Rules deploy successfully

---

## Step 5.2 — Cloud Functions

**Include in prompt:**
- `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/08. Firebase Cloud Functions.md`

**Prompt instruction:**
> Implement the Firebase Cloud Functions for care reminder notifications and subscription webhook handling.

**Validation checkpoint:**
- [ ] All function files exist in `functions/src/`
- [ ] Functions compile: `cd functions && npm run build`
- [ ] Functions deploy successfully

---

# Layer 6: Navigation & Auth Flow

## Step 6.1 — Full Navigation Architecture

**Include in prompt:**
- `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/10. Navigation Architecture.md`

**Prompt instruction:**
> Implement the full navigation structure in RootNavigation.tsx: auth state switching, DrawerNavigator (side menu), MainScreen (4 bottom tabs: Home, Animals, Production, Care), and SideMenu.

**Validation checkpoint:**
- [ ] Root navigation switches between Loading/Auth/Main based on auth state
- [ ] Drawer slides from left when avatar is tapped
- [ ] Bottom tabs render 4 placeholder screens
- [ ] Navigation between screens works

---

## Step 6.2 — Auth & Onboarding

**Include in prompt:**
- `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/12. Auth & Onboarding Screens.md`

**Prompt instruction:**
> Implement the authentication flow using the FirebaseAuth library. Create Login, Register, Loading screens, and Species Selection onboarding. The AuthService wraps FirebaseAuth and is accessed through Bootstrap.

**Validation checkpoint:**
- [ ] Login and Register screens render correctly
- [ ] Firebase auth works via FirebaseAuth.ts
- [ ] Species selection seeds starter animalTypes
- [ ] Logout clears stores and returns to login

---

# Layer 7: Core Features (one prompt per feature)

For each feature, include the relevant feature doc.

## Step 7.1 — Today Dashboard

**Include in prompt:**
- `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/14. Feature - Today Dashboard.md`

**Prompt instruction:**
> Implement the Today Dashboard (Home tab). Rewrite the existing HomeScreen.tsx as a read-only dashboard that aggregates overdue care, due-today care, active withdrawal alerts, and breeding countdowns. Create HomeController.ts and section components (OverdueCareSection, DueTodaySection, WithdrawalAlertSection, BreedingCountdownSection). Care events and active breedings are real-time via onSnapshot stores. Health records are fetched on focus for withdrawal computation using WithdrawalUtility. Include time-of-day greeting, pull-to-refresh, and "All caught up!" empty state. No FAB on this screen.

**Validation checkpoint:**
- [ ] All files exist in `apps/mobile/src/feature/home/` with `HomeScreen.tsx`, `HomeController.ts`, and `component/` directory
- [ ] HomeController uses `useCareStore` and `useBreedingStore` (real-time) and `useHealthStore.fetchHealthRecords` (on focus)
- [ ] Overdue and due-today events derived from `getCareStatus()` in `CareUtility.ts`
- [ ] Active withdrawals computed via `getActiveWithdrawals()` from `WithdrawalUtility.ts`
- [ ] Sections only render when items exist; empty state shows when all sections are empty
- [ ] Pull-to-refresh re-fetches health records only (care + breeding are real-time)
- [ ] Greeting is time-of-day aware (morning/afternoon/evening)
- [ ] Controller + Screen pattern followed; no Firebase imports in controller or screen
- [ ] TypeScript compiles with no errors

---

## Step 7.2 — Animal Profiles

**Include in prompt:**
- `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/15. Feature - Animal Profiles.md`

**Prompt instruction:**
> Implement the Animals tab with animal list, CRUD, and animal detail hub. Create AnimalListScreen with SectionList grouped by animalType, search bar, and state filter chips. Create AnimalDetailScreen with tabs (Timeline, Health, Breeding, Care, Notes, Weight) and AnimalDetailHeader with photo, name, breed, age, and state badge. Create CreateAnimalScreen and EditAnimalScreen with photo picker, species/breed dropdowns, gender selector, and ParentSelector for sire/dam linking. AnimalService uses onSnapshot for real-time animal list. Photo upload uses @react-native-firebase/storage directly in the service. Free tier limited to 10 animals.

**Validation checkpoint:**
- [ ] All files exist in `apps/mobile/src/feature/animal/` with `screen/`, `component/`, and `service/` directories
- [ ] `AnimalService.subscribe()` uses `onSnapshot` with `admin.deleted == false` filter
- [ ] `AnimalService` registered in `Bootstrap.ts` as `bsAnimalService`
- [ ] Animal list groups by `animalType` and supports search + state filtering
- [ ] Animal Detail has 6 tabs (Timeline, Health, Breeding, Care, Notes, Weight) with context-aware FAB
- [ ] Create/Edit screens include photo picker, species/breed dropdowns, and ParentSelector
- [ ] `ParentSelector` filters by matching animalType and gender
- [ ] Controllers call `bsAnimalService` from Bootstrap; no Firebase imports in controllers or screens
- [ ] Subscription gating: free tier capped at 10 animals
- [ ] TypeScript compiles with no errors

---

## Step 7.3 — Care Reminders

**Include in prompt:**
- `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/16. Feature - Care Reminders.md`

**Prompt instruction:**
> Implement the Care tab with cross-animal care schedule and care event CRUD. Create CareListScreen with events grouped by status (Overdue red, Due Today amber, Upcoming green, Future gray) and animal filter dropdown. Create CareEventDetailScreen showing full event info with "Mark Complete" button. Create CreateCareEventScreen with animal selector, type toggle (recurring/single), cycle days, and due date. CareService uses onSnapshot for real-time updates and firestore().batch() for atomic completion + next-recurring-event creation. Next due date calculated from completedDate, not original dueDate. Free tier limited to 3 care events per animal.

**Validation checkpoint:**
- [ ] All files exist in `apps/mobile/src/feature/care/` with `screen/`, `component/`, and `service/` directories
- [ ] `CareService.subscribe()` uses `onSnapshot` with `admin.deleted == false` filter
- [ ] `CareService.completeCareEvent()` uses `firestore().batch()` for atomic completion + next event creation
- [ ] Recurring events create next occurrence from `completedDate` (not original `dueDate`)
- [ ] `createdNextRecurringEvent` flag prevents duplicate next-event creation
- [ ] One-time events (`careSingle`) do not create next occurrences
- [ ] Care list renders 4 status sections: Overdue, Due Today, Upcoming, Future
- [ ] `CareService` registered in `Bootstrap.ts` as `bsCareService`
- [ ] Controllers call `bsCareService` from Bootstrap; no Firebase imports in controllers or screens
- [ ] Subscription gating: free tier capped at 3 care events per animal
- [ ] TypeScript compiles with no errors

---

## Step 7.4 — Health Records & Withdrawal Calculator

**Include in prompt:**
- `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/17. Feature - Health Records & Withdrawal Calculator.md`

**Prompt instruction:**
> Implement the health records system with all 6 record types (vaccination, medication, deworming, vetVisit, illness, injury) and the withdrawal period calculator. Create CreateHealthRecordScreen with HealthRecordTypeSelector and type-specific field components (VaccinationFields, MedicationFields, DewormingFields, VetVisitFields, IllnessInjuryFields). Create HealthRecordDetailScreen with WithdrawalStatusCard. Create WithdrawalBanner for Animal Detail. HealthService uses standard get() queries (no onSnapshot) and receives ICareService via constructor injection to auto-create care reminders when a vaccination has vaccineNextDueDate. Withdrawal is ACTIVE (red) or CLEAR (green) — no ambiguity. Free tier has no access to health records.

**Validation checkpoint:**
- [ ] All files exist in `apps/mobile/src/feature/health/` with `screen/`, `component/`, and `service/` directories
- [ ] `HealthService` constructor accepts `ICareService` for cross-service vaccination→care linking
- [ ] `HealthService` registered in `Bootstrap.ts` as `bsHealthService` with `careService` injected
- [ ] 6 type-specific field components render conditionally based on selected `recordType`
- [ ] Medication and deworming fields include withdrawal period days and withdrawal type (prominently displayed)
- [ ] `WithdrawalStatusCard` shows ACTIVE (red) or CLEAR (green) states
- [ ] `WithdrawalBanner` displays on Animal Detail when active withdrawals exist
- [ ] `fetchAllWithdrawalRecords` runs two queries (medication + deworming) and deduplicates
- [ ] No `onSnapshot` — health records fetched on demand
- [ ] Controllers call `bsHealthService` from Bootstrap; no Firebase imports in controllers or screens
- [ ] Subscription gating: free tier has no access to health records
- [ ] TypeScript compiles with no errors

---

## Step 7.5 — Breeding Manager

**Include in prompt:**
- `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/18. Feature - Breeding Manager.md`

**Prompt instruction:**
> Implement the breeding manager with sire/dam pairing, auto-calculated gestation/incubation countdown, birth outcome recording, and offspring auto-linking. Create CreateBreedingRecordScreen with SireSelector (herd or manual name), breeding date, method, and inline gestation preview using species/breed-specific days. Create BreedingRecordDetailScreen with GestationCountdown progress bar for active breedings and offspring list for completed ones. Create RecordBirthOutcomeScreen that uses firestore().batch() to atomically update the breeding record and create offspring Animal documents inheriting type/breed from the dam. BreedingService uses onSnapshot for real-time updates. Free tier has no access to breeding records.

**Validation checkpoint:**
- [ ] All files exist in `apps/mobile/src/feature/breeding/` with `screen/`, `component/`, and `service/` directories
- [ ] `BreedingService.subscribe()` uses `onSnapshot` with `admin.deleted == false` filter
- [ ] `BreedingService.completeBirth()` uses `firestore().batch()` for atomic offspring creation + record update
- [ ] Offspring inherit `animalType`, `breed`, `animalTypeId`, `animalTypeLevel`, `animalBreedId` from dam
- [ ] Offspring get `damId` and `sireId` linked; default names "Baby 1", "Baby 2", etc.
- [ ] `GestationCountdown` shows progress bar, days remaining, and "Overdue by X days" when applicable
- [ ] Gestation days resolved: breed override → species default from `GESTATION_TABLE` → manual entry
- [ ] `SireSelector` supports "Select from herd" (filtered by type + male) and "Enter name manually"
- [ ] Breeding state machine: `active` → `completed` or `failed` (no reverse transitions)
- [ ] `BreedingService` registered in `Bootstrap.ts` as `bsBreedingService`
- [ ] Controllers call `bsBreedingService` from Bootstrap; no Firebase imports in controllers or screens
- [ ] Subscription gating: free tier has no access to breeding records
- [ ] TypeScript compiles with no errors

---

## Step 7.6 — Production Tracking

**Include in prompt:**
- `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/19. Feature - Production Tracking.md`

**Prompt instruction:**
> Implement the Production tab with daily logging for eggs, milk, fiber, honey, meat, and other production types. Create ProductionListScreen with QuickLogEntry (row of tappable type icons for one-tap access), ProductionSummaryCard (daily/weekly/monthly aggregation), type filter chips, and recent log list. Create CreateProductionLogScreen with production type selector, quantity + unit input (default units per type), date picker, and per-animal or per-group log mode toggle. ProductionService uses standard get() queries (no onSnapshot). Store uses fetch-on-demand with useFocusEffect. Free tier has no access to production logs.

**Validation checkpoint:**
- [ ] All files exist in `apps/mobile/src/feature/production/` with `screen/`, `component/`, and `service/` directories
- [ ] `ProductionService` uses `get()` queries (no `onSnapshot`)
- [ ] `ProductionStore` uses explicit `fetch` actions (no `subscribe`)
- [ ] Controller uses `useFocusEffect` to refetch on screen focus
- [ ] `QuickLogEntry` renders a row of production type icons for one-tap navigation
- [ ] `ProductionSummaryCard` shows daily, weekly, and monthly aggregation
- [ ] Default units applied per production type (eggs→count, milk→gallons, fiber→lbs, etc.)
- [ ] Per-animal and per-group modes are mutually exclusive (`animalId` or `groupName`)
- [ ] `ProductionService` registered in `Bootstrap.ts` as `bsProductionService`
- [ ] Controllers call `bsProductionService` from Bootstrap; no Firebase imports in controllers or screens
- [ ] Subscription gating: free tier has no access to production logs
- [ ] TypeScript compiles with no errors

---

## Step 7.7 — Notes & Observations

**Include in prompt:**
- `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/20. Feature - Notes & Observations.md`

**Prompt instruction:**
> Implement the notes system with quick timestamped entries per animal, tag-based categorization, and optional photo attachment. Create CreateNoteScreen with multiline text input, tag selector chips (Health, Behavior, Breeding, Feed, Production, General with distinct colors), and PhotoPicker. Create NoteDetailScreen showing full text, tags, photo, date with relative time, and delete button with confirmation. NoteService uses standard get() queries (no onSnapshot) with photo upload via @react-native-firebase/storage. Soft-delete only (admin.deleted = true). Notes display on the AnimalNotesTab in Animal Detail. Free tier has no access to notes.

**Validation checkpoint:**
- [ ] All files exist in `apps/mobile/src/feature/notes/` with `screen/` and `service/` directories
- [ ] `NoteService` implements `INoteService` interface with `fetchNotesByAnimal`, `createNote`, `deleteNote`
- [ ] `NoteService` uses `get()` queries (no `onSnapshot`)
- [ ] Photo upload uses `@react-native-firebase/storage` with path `homestead/{homesteadId}/note/{noteId}/photo.jpg`
- [ ] Tag selector supports multi-select with 6 color-coded tags
- [ ] Delete uses soft-delete (`admin.deleted = true`), not hard delete
- [ ] `NoteDetailScreen` shows full content, tags, photo, relative time, and delete with confirmation
- [ ] `NoteService` registered in `Bootstrap.ts` as `bsNoteService`
- [ ] Controllers call `bsNoteService` from Bootstrap; no Firebase imports in controllers or screens
- [ ] Subscription gating: free tier has no access to notes
- [ ] TypeScript compiles with no errors

---

# Layer 8: Settings & Advanced Features

## Step 8.1 — Profile & Settings

**Include in prompt:**
- `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/21. Feature - Profile & Settings.md`

**Prompt instruction:**
> Implement the Profile & Settings feature. Create ProfileScreen (avatar, name, email, stats, menu items, logout with confirm dialog), EditProfileScreen (avatar upload via ProfileService, name fields via UserService), SettingsScreen (notification toggle, weight/milk unit selectors stored in MMKV via createMMKV() directly), and SendFeedbackScreen (star rating + text, submitted via ProfileService to root-level feedback collection). ProfileService handles only avatar upload and feedback submission — user CRUD is in UserService. Logout calls bsAuthService.signOut() and resetAllStores(). Register bsProfileService in Bootstrap.ts. Follow Controller + Screen pattern, style with Uniwind.

**Validation checkpoint:**
- [ ] All screens exist in `apps/mobile/src/feature/profile/screen/`: ProfileScreen, EditProfileScreen, SettingsScreen, SendFeedbackScreen with matching Controllers
- [ ] `IProfileService.ts` and `ProfileService.ts` exist in `apps/mobile/src/feature/profile/service/`
- [ ] ProfileService handles only avatar upload (Firebase Storage) and feedback submission — not user CRUD
- [ ] Settings stored in MMKV via `createMMKV()` directly (no ICacheService wrapper)
- [ ] Logout triggers `bsAuthService.signOut()` and `resetAllStores()`
- [ ] `bsProfileService` exported from `Bootstrap.ts`
- [ ] TypeScript compiles with no errors

---

## Step 8.2 — Customization Engine

**Include in prompt:**
- `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/22. Feature - Customization Engine.md`

**Prompt instruction:**
> Implement the Customization Engine feature. Create CustomizationHomeScreen (list of animal types with real-time updates via onSnapshot), AnimalTypeDetailScreen (breeds, care templates, event templates), EditAnimalTypeScreen (name + color chips), EditBreedScreen (name + gestation days override), and EditCareTemplateScreen (name, recurring/single type, cycle days, contact info). AnimalTypeService calls @react-native-firebase/firestore directly via homestead subcollection. Include seedStarterPlaybook using firestore().batch() for atomic writes during onboarding. Create StarterPlaybooks.ts with preset data for Chicken, Goat, Cattle, Sheep, Pig, Rabbit, Horse, Duck. Register bsAnimalTypeService in Bootstrap.ts. Follow Controller + Screen pattern, style with Uniwind.

**Validation checkpoint:**
- [ ] All screens and components exist in `apps/mobile/src/feature/customization/screen/` and `component/`
- [ ] `IAnimalTypeService.ts` and `AnimalTypeService.ts` exist in `apps/mobile/src/feature/customization/service/`
- [ ] `StarterPlaybooks.ts` exists in `apps/mobile/src/feature/customization/data/` with all species data
- [ ] `onSnapshot` used for animal types list (real-time updates)
- [ ] `seedStarterPlaybook` uses `firestore().batch()` for atomic writes
- [ ] Subcollections (breed, careTemplate, eventTemplate) are fetched on demand, not via onSnapshot
- [ ] `bsAnimalTypeService` exported from `Bootstrap.ts`
- [ ] TypeScript compiles with no errors

---

## Step 8.3 — Subscription & Paywall

**Include in prompt:**
- `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/23. Feature - Subscription & Paywall.md`

**Prompt instruction:**
> Implement the Subscription & Paywall feature. Create SubscriptionGate.ts utility with gate functions (checkAnimalLimit, checkCareEventLimit, checkProFeature, checkFarmFeature). Create PaywallScreen (triggered at moment of action with feature name and reason, shows tier comparison and purchase buttons), SubscriptionScreen (current plan display, upgrade/manage/restore buttons). Create SubscriptionService that wraps the existing RevenueCatInAppPurchases class — mapping tier1=free, tier2=pro, tier3=farm. SubscriptionService does NOT call Firebase directly. Create TierCard, FeatureComparisonTable, and PaywallBanner components. Register bsSubscriptionService in Bootstrap.ts (inject RevenueCatInAppPurchases via constructor). Follow Controller + Screen pattern, style with Uniwind.

**Validation checkpoint:**
- [ ] `SubscriptionGate.ts` exists in `apps/mobile/src/feature/subscription/util/` with all gate functions
- [ ] All screens exist in `apps/mobile/src/feature/subscription/screen/`: PaywallScreen, SubscriptionScreen with matching Controllers
- [ ] All components exist in `apps/mobile/src/feature/subscription/component/`: TierCard, FeatureComparisonTable, PaywallBanner
- [ ] `SubscriptionService.ts` exists in `apps/mobile/src/feature/subscription/service/`
- [ ] SubscriptionService wraps RevenueCatInAppPurchases — does NOT import @react-native-firebase/*
- [ ] Tier mapping correct: tier1=free, tier2=pro, tier3=farm
- [ ] `bsSubscriptionService` exported from `Bootstrap.ts` with RevenueCat injected via constructor
- [ ] TypeScript compiles with no errors

---

# Layer 9: Scripts & Tooling

## Step 9.1 — Scripts & Build Tooling

**Include in prompt:**
- `/Users/molnarr2/Projects/homestead2/vault/homestead-vault/01_research/01_plan/00_gen/03. TDD/24. Scripts & Build Tooling.md`

**Prompt instruction:**
> Create all project scripts in the scripts/ directory: run-ios.sh, run-android.sh, clean-ios.sh, clean-android.sh, deploy-rules.sh, deploy-functions.sh, deploy-all.sh, and test.sh. All scripts use set -e and relative paths via $(dirname "$0"). Create firebase.json at the project root pointing to firebase-rule/ for rules/indexes and functions/ for Cloud Functions. Make all scripts executable with chmod +x.

**Validation checkpoint:**
- [ ] All scripts exist in `scripts/`: run-ios.sh, run-android.sh, clean-ios.sh, clean-android.sh, deploy-rules.sh, deploy-functions.sh, deploy-all.sh, test.sh
- [ ] All scripts are executable (`chmod +x`)
- [ ] All scripts use `set -e` and relative paths via `$(dirname "$0")`
- [ ] `firebase.json` exists at project root with correct paths to firebase-rule/ and functions/
- [ ] Scripts reference `apps/mobile/` for the monorepo structure

---

# Rules for the LLM

Include these rules in **every prompt session**:

```
## Rules
1. Follow the document instructions exactly. Do not invent features not described.
2. Use the exact file paths and naming conventions specified. All paths start with apps/mobile/src/.
3. Every schema must include `admin: AdminObject` with Tstamp timestamps (Firebase Timestamps, NOT ISO strings).
4. Use Uniwind (Tailwind CSS for React Native) className for all styling. Do NOT use react-native-paper.
5. Use Zustand for state. Do NOT use Redux or Context for state management.
6. Follow the Controller + Screen pattern: Controller is a custom hook, Screen is pure UI.
7. Services are the ONLY files that import @react-native-firebase/* (except FirebaseAuth.ts).
8. UI and Controllers NEVER import Firebase, MMKV, or other 3rd-party data libraries directly.
9. All Firestore queries filter `admin.deleted == false` unless explicitly stated otherwise.
10. Include userId on all documents (animal, care, health, breeding, production, note, weight).
11. Do not add comments or documentation unless the document specifies it.
12. TypeScript strict mode. No `any` types unless absolutely necessary.
13. AdminObject dates use Firebase Timestamps (Tstamp type). Domain dates (dueDate, birthday, etc.) use ISO 8601 strings with date-fns.
14. Subscription gating enforced client-side with RevenueCat entitlements.
15. Use onSnapshot for real-time listeners on: animals, care events, active breedings, active withdrawals.
16. Services return Promise<IResult> for all mutations.
17. Services are instantiated in Bootstrap.ts and exported as bs{ServiceName}.
18. Schema files contain ONLY interface + {entity}_default() function. No _get(), _set(), _collection() helpers.
```

---

# Quick Reference: What to Include Per Prompt

| Step | Documents to Include |
|------|---------------------|
| 2.1 | `03. TDD/02. Shared Schema Definitions.md` + `03. TDD/03. Utility & Foundation Layer.md` |
| 3.1 | `03. TDD/04. Services Layer.md` |
| 4.1 | `03. TDD/13. Foundation Context Reference.md` + `03. TDD/06. State Management with Zustand.md` |
| 5.1 | `03. TDD/07. Firestore Schema & Security Rules.md` |
| 5.2 | `03. TDD/08. Firebase Cloud Functions.md` |
| 6.1 | `03. TDD/10. Navigation Architecture.md` |
| 6.2 | `03. TDD/12. Auth & Onboarding Screens.md` |
| 7.1 | `03. TDD/14. Feature - Today Dashboard.md` |
| 7.2 | `03. TDD/15. Feature - Animal Profiles.md` |
| 7.3 | `03. TDD/16. Feature - Care Reminders.md` |
| 7.4 | `03. TDD/17. Feature - Health Records & Withdrawal Calculator.md` |
| 7.5 | `03. TDD/18. Feature - Breeding Manager.md` |
| 7.6 | `03. TDD/19. Feature - Production Tracking.md` |
| 7.7 | `03. TDD/20. Feature - Notes & Observations.md` |
| 8.1 | `03. TDD/21. Feature - Profile & Settings.md` |
| 8.2 | `03. TDD/22. Feature - Customization Engine.md` |
| 8.3 | `03. TDD/23. Feature - Subscription & Paywall.md` |
| 9.1 | `03. TDD/24. Scripts & Build Tooling.md` |
