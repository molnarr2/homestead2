# References

app: /Users/molnarr2/src/antioch/antioch

# Instructions

The app currently has the subscription on the user record, but that's wrong instead it should be on the Homestead record and it should be like the Antioch app.

we want to analyze the Antioch app on the ministry level for how it overrides the RevenueCat tier level and do the same for our app at the homestead level. Within our app, like the Antioch app, there should be a single service that is the source of truth, which looks at the homestead record as a source of truth. And it should have the same logic as the Antioch app does to determine what the tier level is. I.e. override as priority and then the revenue cat. It should also have the same logic for when the revenuecat gets updated.

Do note that if the user is not role of owner than it shouldn't do the revenuecat logic since only the owner is the one that handles the subscription.