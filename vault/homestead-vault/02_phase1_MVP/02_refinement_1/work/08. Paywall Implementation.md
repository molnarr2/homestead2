Use the tech spec skill:
* /Users/molnarr2/src/homestead2/vault/homestead-vault/02_phase1_MVP/02_refinement_1/
* prefix `13_`

We are working on the Pay wall integration into the app. We want to create a single source of truth Service that is used to determine the tier level. The homestead record has the fields. There are two fields one will be for RevenueCat the second one is a override field. The override field is the first source of truth, and then the RevenueCat one is the second one.

We want to create a single shared component for the Paywall. It should be a model that pops up, comes up from the bottom of the screen up, and it has two tiers. 

We will be creating new product IDs: `homestead_standard_monthly`, `homestead_pro_monthly`

The only gate limit we are going to do is animal head count at 12 for free tier.

Standard - 50 animals
Pro - unlimited animals
