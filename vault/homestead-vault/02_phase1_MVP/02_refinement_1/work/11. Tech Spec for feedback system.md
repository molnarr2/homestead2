The old Homestead app (`/Users/molnarr2/src/homestead-native`) has a feedback system built into it. We want to look at it and build something similar.

Should be built around the new analytics engine for core events. Once a user hit so many core events then it should show the feedback screen.

The feedback model should have the following:
* the ability to tap one to five stars on what you'd give the app
* a section for the following what do you like best about the app and it has five areas of the app to choose from?
* a section the following five areas of the app that needs improvement.
* a section: field that could be freeform text entered so they can give back whatever kind of feedback they want.

this feedback model should come up after the following core events happened:
* 10, 25, 50, 100, then every 100 interval

AND This feedback model should be accessible via the side menu

Like the old way this data gets send to the feedback table in Firebase.

---

The other thing we want is to trigger app review of the app ie review in the Apple/Google store. This should happen after 20 core events and then 50 and then every 100 core events.